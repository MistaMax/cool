#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    
    return 0;
}

char *str_mixer(char *s1, char *s2)
{
    int len1=0,len2=0,end=0,i=0,order=1;
    len1=strlen(s1);
    len2=strlen(s2);
    
}
